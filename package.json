{
  "name": "bdapipelis",
  "version": "1.0.0",
  "description": "Proyecto monorepo que integra el backend (Node.js + Express + MongoDB) y el frontend (React) de la aplicación BD API Pelis.",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "author": "MMUNOZAC1",
  "license": "MIT"
}
